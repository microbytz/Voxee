import { ChatContainer } from '@/components/chat/chat-container';

export default function VoxeePage() {
  return (
    <main className="flex h-full flex-col items-center justify-center">
      <ChatContainer />
    </main>
  );
}
